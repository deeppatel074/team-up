doctype html
html
    head
        meta(name="viewport" content="width=device-width")
        meta(http-equiv="Content-Type" content="text/html; charset=UTF-8")
        title
            block subject

    body(style="font-family: sans-serif; color: #5d5d5d;")
        row
            columns &nbsp;
            columns(class="container")
                div(class="content" style=" background: #f9f9f9; padding: 40px;")
                    span(class="preheader")
                        block preview
                    row(class="main")
                        columns(class="wrapper")
                            row
                                columns
                                    p
                                    block content
                                        h1 Hello #{userName},  
                                        p #{name} invited you as an team member on workspace #{workspaceName}, 
                                        p Click below button to accept invitation
                                        p 
                                            a(href=`${url}` target='_blank') Accept Invitation
                                        br
                                        br
                            row
                                columns
                                    | Cheers,
                                    br
                                    | Team Up.

            columns &nbsp;